# Swift Package Manager / Xcode build output
.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
Package.resolved

# Model weights & compiled models (never commit — download at runtime)
*.safetensors
*.gguf
*.bin
*.mlmodel
*.mlmodelc
*.mlpackage
# MobileSAM prompt-encoder weights (release artifact bundled into the example app)
mobile_sam_prompt_encoder_weights.json
# YOLOE CLIP BPE vocabulary (release artifact bundled into the example app)
clip_vocab.json

# macOS
.DS_Store

# Generated Xcode project for the example app (reproduce with `xcodegen generate`)
Examples/ShelfScout/ShelfScout.xcodeproj/
# Entitlements file generated by XcodeGen from project.yml
Examples/ShelfScout/ShelfScout.entitlements

# Internal docs — keep local, never publish (strategy brief, handoffs, task directives)
PROJECT_BRIEF.md
HANDOFF.md
NEXT_TASK_ROI_ZOOM.md
NEXT_TASK_DESCRIBE_AND_POINT.md
# Local article/draft (not part of the library repo)
crowd-demo-article.md
