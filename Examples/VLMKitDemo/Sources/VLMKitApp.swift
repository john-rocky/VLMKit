import SwiftUI

/// VLMKit — an on-device showcase for VLMKit's structured fan-out recipes. Pick a
/// demo, give it a photo, and watch an Apple framework + a local VLM turn pixels
/// into structured, point-able results. All inference runs on-device via MLX.
@main
struct VLMKitApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
